// /admin/sync — Skeleton (Sprint-1-AP-6). Implementierung in Sprint 6.

import React from 'react';
import AdminPlaceholder from './AdminPlaceholder';

export default function AdminSync() {
  return (
    <AdminPlaceholder
      title="Sync-Verwaltung"
      sprint={6}
      description="Status und Ausloeser fuer Drittsysteme (ThemiSoft, myYolo, SimplyBook)."
    />
  );
}
